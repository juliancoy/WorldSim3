#include "status_api_context_builder.h"

StatusApiContext makeStatusApiContext(const StatusApiContextFactoryInput& input) {
    StatusApiContext ctx;
    ctx.app_version = input.app_version;
    ctx.protocol_version = input.protocol_version;
    ctx.tile_cache_max = input.tile_cache_max;
    ctx.stop = input.stop;
    ctx.layers = input.layers;
    ctx.duckdb_analytics = input.duckdb_analytics;
    ctx.unified_parcels = input.unified_parcels;
    ctx.map_filter_state = input.map_filter_state;
    ctx.active_filter_result_set = input.active_filter_result_set;
    ctx.query_layers = input.query_layers;
    ctx.active_filter_status = input.active_filter_status;
    ctx.time_cube_service = input.time_cube_service;
    ctx.screenshot = input.screenshot;
    ctx.status_mutex = input.status_mutex;
    ctx.layer_states = input.layer_states;
    ctx.layer_fill_mutex = input.layer_fill_mutex;
    ctx.layer_fill_enabled = input.layer_fill_enabled;
    ctx.layer_hover_enabled = input.layer_hover_enabled;
    ctx.layer_inspect_enabled = input.layer_inspect_enabled;
    ctx.layer_heatmap_enabled = input.layer_heatmap_enabled;
    ctx.hydration_started_at = input.hydration_started_at;
    ctx.hydrated_count = input.hydrated_count;
    ctx.triangulated_count = input.triangulated_count;
    ctx.prof_tile_cache_size = input.prof_tile_cache_size;
    ctx.current_zoom_state = input.current_zoom_state;
    ctx.current_lon_state = input.current_lon_state;
    ctx.current_lat_state = input.current_lat_state;
    ctx.visible_vacant_parcels_last_frame = input.visible_vacant_parcels_last_frame;
    ctx.vacant_parcels_matched_total = input.vacant_parcels_matched_total;
    ctx.vacant_parcels_with_geometry_total = input.vacant_parcels_with_geometry_total;
    ctx.vacant_parcels_triangulated_renderable_total = input.vacant_parcels_triangulated_renderable_total;
    ctx.perf_frame_ms_avg = input.perf_frame_ms_avg;
    ctx.perf_frame_ms_last = input.perf_frame_ms_last;
    ctx.perf_fps_avg = input.perf_fps_avg;
    ctx.ui_left_panel_frac = input.ui_left_panel_frac;
    ctx.ui_right_panel_frac = input.ui_right_panel_frac;
    ctx.render_fill_attempts_last_frame = input.render_fill_attempts_last_frame;
    ctx.render_fill_success_last_frame = input.render_fill_success_last_frame;
    ctx.render_fill_no_triangles_last_frame = input.render_fill_no_triangles_last_frame;
    ctx.render_fill_bad_indices_last_frame = input.render_fill_bad_indices_last_frame;
    ctx.api_layer_mutex = input.api_layer_mutex;
    ctx.api_layer_enable_cmds = input.api_layer_enable_cmds;
    ctx.api_layer_fill_cmds = input.api_layer_fill_cmds;
    ctx.api_layer_download_cmds = input.api_layer_download_cmds;
    ctx.api_zoom_cmd = input.api_zoom_cmd;
    ctx.api_lon_cmd = input.api_lon_cmd;
    ctx.api_lat_cmd = input.api_lat_cmd;
    ctx.api_ui_cmd_seq = input.api_ui_cmd_seq;
    ctx.api_ui_cmd_kind = input.api_ui_cmd_kind;
    ctx.api_ui_cmd_x = input.api_ui_cmd_x;
    ctx.api_ui_cmd_y = input.api_ui_cmd_y;
    ctx.api_ui_cmd_button = input.api_ui_cmd_button;
    ctx.api_ui_cmd_scroll_y = input.api_ui_cmd_scroll_y;
    ctx.api_control_mutex = input.api_control_mutex;
    ctx.api_filter_control_cmd = input.api_filter_control_cmd;
    ctx.api_query_control_cmds = input.api_query_control_cmds;
    ctx.layer_profile_mutex = input.layer_profile_mutex;
    ctx.layer_profile_snapshot = input.layer_profile_snapshot;
    ctx.profile_mutex = input.profile_mutex;
    ctx.profile_samples = input.profile_samples;
    ctx.profile_sample_pos = input.profile_sample_pos;
    ctx.profile_sample_count = input.profile_sample_count;
    ctx.profile_reset_generation = input.profile_reset_generation;
    ctx.prof_ui_ms_last = input.prof_ui_ms_last;
    ctx.prof_owner_ms_last = input.prof_owner_ms_last;
    ctx.prof_tile_ms_last = input.prof_tile_ms_last;
    ctx.prof_layer_ms_last = input.prof_layer_ms_last;
    ctx.prof_heatmap_ms_last = input.prof_heatmap_ms_last;
    ctx.prof_overlay_ms_last = input.prof_overlay_ms_last;
    ctx.prof_present_ms_last = input.prof_present_ms_last;
    ctx.prof_tiles_drawn_last = input.prof_tiles_drawn_last;
    ctx.prof_features_considered_last = input.prof_features_considered_last;
    ctx.prof_features_drawn_last = input.prof_features_drawn_last;
    ctx.prof_heat_samples_last = input.prof_heat_samples_last;
    ctx.prof_retired_textures = input.prof_retired_textures;
    ctx.prof_projection_world_ring_cache_entries = input.prof_projection_world_ring_cache_entries;
    ctx.prof_projection_world_extent_cache_entries = input.prof_projection_world_extent_cache_entries;
    ctx.prof_projection_cache_generation = input.prof_projection_cache_generation;
    ctx.prof_heatmap_gpu_splat_active = input.prof_heatmap_gpu_splat_active;
    ctx.prof_heatmap_high_quality = input.prof_heatmap_high_quality;
    ctx.prof_heatmap_cache_valid = input.prof_heatmap_cache_valid;
    ctx.prof_heatmap_texture_resident = input.prof_heatmap_texture_resident;
    ctx.prof_heatmap_async_inflight = input.prof_heatmap_async_inflight;
    ctx.prof_heatmap_cache_key = input.prof_heatmap_cache_key;
    ctx.prof_heatmap_texture_cache_entries = input.prof_heatmap_texture_cache_entries;
    return ctx;
}
